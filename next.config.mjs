/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  compress: true,
  images: {
    formats: ["image/avif", "image/webp"],
    // Real project photos can be served from these remote hosts once supplied.
    remotePatterns: [],
  },
  async headers() {
    return [
      {
        // Long-cache static, fingerprinted gallery assets for better Core Web Vitals.
        source: "/images/:path*",
        headers: [
          {
            key: "Cache-Control",
            value: "public, max-age=31536000, immutable",
          },
        ],
      },
    ];
  },
};

export default nextConfig;
